import { CreckPermissionDirectiveDirective } from './creck-permission-directive.directive';

describe('CreckPermissionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CreckPermissionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
